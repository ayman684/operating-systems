#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    // Correctly separated variable declarations
    char mainDirectory[] = "C:/Users/itssk/OneDrive/Desktop";
    char subDirectory[] = "os";
    char fileName[] = "example.txt";
    
    char filePath[200];
    char mainDirPath[200];

    // Combine main directory and subdirectory safely
    snprintf(mainDirPath, sizeof(mainDirPath), "%s/%s", mainDirectory, subDirectory);

    // Combine the directory path with the file name
    snprintf(filePath, sizeof(filePath), "%s/%s", mainDirPath, fileName);

    // Attempt to open/create the file for writing
    FILE *file = fopen(filePath, "w");
    
    if (file == NULL) { 
        printf("Error creating file.\n");
        return 1;
    }

    // Write content and close the file
    fprintf(file, "This is an example file content.");
    fclose(file);

    printf("File created successfully: %s\n", filePath);

    return 0;
}
output:
File created successfully

Process exited after 1.379 seconds with return value 0
Press any key to continue . . .
