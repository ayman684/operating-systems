#include<stdio.h>
#include<string.h>

int main()
{
    char fn[100], pat[100], temp[200];
    FILE *fp;

    printf("\nEnter file name : ");
    scanf("%s", fn);
    printf("Enter the pattern: ");
    scanf("%s", pat);

    fp = fopen(fn, "r");
    if(fp == NULL) {
        printf("File not found\n");
        return 1;
    }

    while(fgets(temp, sizeof(temp), fp)!= NULL) {
        // fgets includes '\n', so remove it for exact match
        temp[strcspn(temp, "\n")] = 0;
        if(strcmp(temp, pat) == 0) {
            printf("%s\n", temp);
        }
    }
    fclose(fp);
    return 0;
}
output:
Enter file name : test.txt
Enter the pattern: Hello, World!
Hello, World!
